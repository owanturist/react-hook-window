module.exports = {
  plugins: [
    {
      resolve: 'smooth-doc',
      options: {
        name: 'React Hook Window',
        description: "Don't render what users won't see",
        siteUrl: 'https://example.com'
      }
    }
  ]
}
